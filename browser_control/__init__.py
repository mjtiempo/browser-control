"""browser-control — drive a Chromium over CDP on a managed profile of its own.

The importable package: `browser_control.lib` is the logic (transport, the
managed profile, the browser lifecycle), `browser_control.cli` is the argv
adapter. `browser-control-cli` is the command.
"""

__version__ = "0.1.0"
